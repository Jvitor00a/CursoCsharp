/*using System.Collections;

var arrayList = new ArrayList();
arrayList.Add(1); //0
arrayList.Add("João"); //1
arrayList.Add(true); //2

Console.WriteLine(arrayList[1]); // Serve para imprimir somente um item de dentro do ArrayList

// arrayList.RemoveAt(1); --> Serve para remover um item de dentro da lista quando usar o Foreach

// arrayList = new ArrayList(); 
// arreyList.Clear();         Esses dois servem para limpar o ArrayList

foreach(var item in arrayList)
{
    Console.WriteLine(item);      // Assim voce percorre e imprime todo o array
} */