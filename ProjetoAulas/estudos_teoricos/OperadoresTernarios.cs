/*int numero1 = 1;
var numero2 = 2;

bool ativo = true;
string status = ativo ? "Cadastro ativo" : "Cadastro Inativo";
Console.WriteLine(status);

Usado em codigos simples e de baixa logica, usa no lugar de um possivel if-else
*/